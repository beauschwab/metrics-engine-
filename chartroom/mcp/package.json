{
  "name": "chartroom-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Chartroom over MCP — thin: validate, call the server, shape. No business logic here.",
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "chartroom-spec": "*",
    "zod": "^3.25.76"
  }
}
