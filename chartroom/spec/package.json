{
  "name": "chartroom-spec",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Dashboard spec DSL: Zod schemas, canonical form, diff, JSON Patch, linter. Pure — no fetch, no fs, no React.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
