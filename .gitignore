node_modules/
dist/
*.tsbuildinfo

# Turborepo's local task cache and run logs.
.turbo/
.DS_Store
test-results/
playwright-report/
blob-report/
keel.db
keel.db-shm
keel.db-wal
chartroom.db
chartroom.db-shm
chartroom.db-wal

# Python bytecode from running the client examples.
__pycache__/
*.pyc

# The agent's virtualenv — `npm run setup:agent` builds it.
.venv/
